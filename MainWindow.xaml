<Window x:Class="Smudge_Timer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Smudge_Timer"
        mc:Ignorable="d"
        Title="Smudge Timer by TKronix" Height="400" Width="600" MinWidth="100" MinHeight="150">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Add the DarkTheme.xaml here -->
                <ResourceDictionary Source="DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </Window.Resources>
    <Grid>
        <Viewbox Stretch="Uniform" StretchDirection="Both">
            <Grid>
                <Label x:Name="timerLabel" Content="00:00" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#512BD4"/>
            </Grid>
        </Viewbox>
        <Button x:Name="startButton" Content="Start" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20"/>
    </Grid>
</Window>
